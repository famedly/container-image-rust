---
name: Docker

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*.*.*' ]
  pull_request:

jobs:
  docker:
    uses: famedly/github-workflows/.github/workflows/docker.yml@46d35e86fb3265536f933828cb57c0d14c5b2bb8
    with:
      push: ${{ github.event_name != 'pull_request' }} # Always build, don't publish on pull requests
